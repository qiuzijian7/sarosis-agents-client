# TDB-AM Memory (Capability Plugin)

`agentCapabilities.memory` provider，作为 TencentDB-Agent-Memory gateway 的薄桥。
真实进程管理与 UI 在姊妹扩展 `tdb-am-viewer` 内，本扩展仅 `~150` 行代码。

参见 `../tdb-am-viewer/USAGE.md` 的总文档。

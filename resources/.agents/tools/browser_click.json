{
  "name": "browser_click",
  "description": "Click an element on the page identified by its reference string from a snapshot.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "ref": {
        "type": "string",
        "description": "Element reference from snapshot to click"
      }
    },
    "required": [
      "ref"
    ]
  },
  "category": "browser",
  "source": "hermes-bundled"
}